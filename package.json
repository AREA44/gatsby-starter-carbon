{
  "name": "gatsby-starte-carbon",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "dev:clean": "gatsby clean && gatsby develop -H 0.0.0.0",
    "build": "gatsby build",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "publish": "gatsby build --prefix-paths"
  },
  "dependencies": {
    "gatsby": "4.12.1",
    "gatsby-theme-carbon": "2.3.0",
    "react": "18.0.0",
    "react-dom": "18.0.0"
  }
}
